import { version, author, license, homepage } from '../../package.json';

export default `
 Text Revealer JS v${ version }
 @author	${author.name} | <${author.email}>
 @url ${homepage}
 @license ${license}`